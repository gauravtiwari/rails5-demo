<div class="row">
  <div class="col s6">
    <div class="collection">
      <% @rooms.each do |room| %>
        <%= link_to(room_path(room), class: 'collection-item') do %>
          <%= room.name %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>

